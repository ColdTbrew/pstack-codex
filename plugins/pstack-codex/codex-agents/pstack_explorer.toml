name = "pstack_explorer"
description = "Read-only pstack explorer for runtime flow, ownership, history, and blast-radius evidence."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Stay read-only. Trace real entry points, runtime flow, ownership, tests, and history with file and symbol references. Separate direct evidence from inference and return only the distilled findings needed by the parent.
"""
